# Nano Agent Plus 依赖

# ── 核心依赖（必需）──────────────────────────────────
openai>=1.0.0          # LLM 后端 (DeepSeek, OpenAI, 等 OpenAI 兼容 API)
python-dotenv>=1.0.0   # .env 环境变量加载

# ── Web UI ──────────────────────────────────────────
fastapi>=0.100.0       # Web 框架
uvicorn>=0.20.0        # ASGI 服务器

# ── 股票工具（可选）──────────────────────────────────
akshare>=1.10.0        # A 股历史数据
yfinance>=0.2.0        # 美股/港股历史数据

# ── 图表工具（可选）──────────────────────────────────
matplotlib>=3.7.0      # Chart, stock chart 图表渲染
numpy>=1.24.0          # Chart 数值计算
scipy>=1.10.0          # Chart 平滑曲线插值
schemdraw>=0.19        # Circuit 电路图渲染

# ── Anthropic 后端（可选）────────────────────────────
# anthropic>=0.40.0
